<%= form_with(model: image_type) do |form| %>
  <% if image_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_type.errors.count, "error") %> prohibited this image_type from being saved:</h2>

      <ul>
        <% image_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
