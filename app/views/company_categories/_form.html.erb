<%= form_with(model: company_category) do |form| %>
  <% if company_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_category.errors.count, "error") %> prohibited this company_category from being saved:</h2>

      <ul>
        <% company_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
